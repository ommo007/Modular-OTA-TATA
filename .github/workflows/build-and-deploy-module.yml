name: '🚀 Build & Deploy OTA Modules'

on:
  push:
    branches: [ main ]
    paths:
      - 'mock_drivers/**'
  workflow_dispatch:

env:
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}

jobs:
  build-and-deploy:
    name: 'Build & Deploy Changed Modules'
    runs-on: ubuntu-latest
    outputs:
      any_modules_deployed: ${{ steps.deploy.outputs.any_modules_deployed }}

    steps:
      - name: '🛒 Checkout Repository'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: '🔍 Detect Changed Module Files'
        id: changed_files
        uses: tj-actions/changed-files@v41
        with:
          files: mock_drivers/**

      - name: '🛠️ Setup ESP32 Toolchain & Dependencies'
        if: steps.changed_files.outputs.any_changed == 'true'
        run: |
          echo "Installing workflow dependencies..."
          sudo apt-get update
          sudo apt-get install -y wget jq
          
          echo "Downloading and installing ESP32 toolchain..."
          cd /tmp
          wget -q https://dl.espressif.com/dl/xtensa-esp32-elf-gcc8_4_0-esp-2021r2-linux-amd64.tar.gz
          tar -xzf xtensa-esp32-elf-gcc8_4_0-esp-2021r2-linux-amd64.tar.gz
          sudo mv xtensa-esp32-elf /opt/
          
          echo "Adding toolchain to PATH for future steps..."
          echo "/opt/xtensa-esp32-elf/bin" >> $GITHUB_PATH
          
          # Manually add to PATH for the CURRENT step
          echo "Exporting toolchain to current PATH..."
          export PATH="/opt/xtensa-esp32-elf/bin:$PATH"
          
          echo "Verifying toolchain installation..."
          # This verification will now succeed
          xtensa-esp32-elf-gcc --version

      - name: '🔧 Prepare Deployment Script'
        if: steps.changed_files.outputs.any_changed == 'true'
        run: |
          chmod +x ./scripts/deploy-to-supabase.sh

      - name: '🏗️ Build & Deploy Changed Modules'
        id: deploy
        if: steps.changed_files.outputs.any_changed == 'true'
        run: |
          deployed_something=false
          changed_modules=$(echo "${{ steps.changed_files.outputs.all_changed_files }}" | tr ' ' '\n' | grep -oP 'mock_drivers/\K[^/]+' | sort -u)
          
          if [ -z "$changed_modules" ]; then
            echo "✅ No module directories were changed. Nothing to deploy."
            echo "any_modules_deployed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          
          echo "📦 Modules to process:"
          echo "$changed_modules"
          overall_status=0
          
          echo "$changed_modules" | while read module; do
            echo "--- Processing Module: $module ---"
            echo "🔨 Building module..."
            if ! make -C "mock_drivers/$module" clean || ! make -C "mock_drivers/$module" build; then
              echo "❌ Build failed for: $module"
              overall_status=1
              continue
            fi
            
            binary_path="mock_drivers/$module/build/$module.bin"
            echo "✅ Build successful. Verifying binary at: $binary_path"
            if [ ! -f "$binary_path" ]; then
                echo "❌ CRITICAL: Build succeeded but binary not found at '$binary_path'!"
                overall_status=1
                continue
            fi

            echo "🚀 Deploying module: $module"
            if ! ./scripts/deploy-to-supabase.sh "$module" "$binary_path"; then
              echo "❌ Deployment script failed for: $module"
              overall_status=1
              continue
            fi
            
            deployed_something=true
            echo "✅ Successfully processed module: $module"
          done
          
          if [ "$overall_status" -ne 0 ]; then
            echo "❌ One or more modules failed to process. Halting workflow."
            exit 1
          fi
          
          echo "any_modules_deployed=$deployed_something" >> "$GITHUB_OUTPUT"
          echo "✅ All changed modules processed successfully."

      - name: '📊 Report Deployment Summary'
        if: always()
        run: |
          echo "=========================================="
          echo "🎯 DEPLOYMENT SUMMARY"
          echo "=========================================="
          if [ "${{ steps.changed_files.outputs.any_changed }}" != 'true' ]; then
            echo "ℹ️ No module changes were detected in the push."
          elif [ "${{ job.status }}" == "success" ]; then
            echo "✅ SUCCESS: All changed modules were built and deployed."
          else
            echo "❌ FAILED: The build and deploy job encountered an error."
          fi
          echo "=========================================="

  validate-deployment:
    name: 'Validate Deployment'
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: success() && needs.build-and-deploy.outputs.any_modules_deployed == 'true'
    env:
      SUPABASE_BUCKET: 'ota-modules'
    steps:
      - name: '✅ Validate Manifest Accessibility'
        run: |
          echo "🔍 Checking if manifest is accessible using authenticated endpoint..."
          sleep 5 
          
          MANIFEST_URL="${{ env.SUPABASE_URL }}/storage/v1/object/${{ env.SUPABASE_BUCKET }}/manifest.json"
          echo "Requesting URL: $MANIFEST_URL"

          http_code=$(curl -s --head -w "%{http_code}" \
            -H "Authorization: Bearer ${{ env.SUPABASE_SERVICE_KEY }}" \
            "$MANIFEST_URL" -o /dev/null)

          if [ "$http_code" -eq 200 ]; then
            echo "✅ Manifest is accessible (HTTP 200)."
            echo "🎉 Deployment validation completed successfully."
          else
            echo "❌ ERROR: Manifest is not accessible. Received HTTP status: $http_code."
            exit 1
          fi```

